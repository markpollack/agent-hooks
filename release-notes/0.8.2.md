# Agent Hooks 0.8.2

Corrective maintenance release restoring the natural public dependency graph for
`agent-hooks-spring`. The release removes direct dependency declarations that were added solely to
align a reactor-generated SBOM with downstream Maven resolution; the certified BURP graph remains
the release authority.
