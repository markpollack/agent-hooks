/**
 * Claude-specific hook events. These events extend the core {@code HookEvent}
 * interface for Claude CLI lifecycle points that have no equivalent in the core API.
 */
@NullMarked
package io.github.markpollack.hooks.claude.event;

import org.jspecify.annotations.NullMarked;
