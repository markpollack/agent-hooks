name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., 0.1.0)'
        required: true
        type: string

jobs:
  release:
    permissions:
      contents: write
    uses: markpollack/build-tools/.github/workflows/maven-central-release.yml@35297f1ade5f47c2925d6dab42a7e2d43bd734d0
    with:
      java-version: '21'
      version: ${{ inputs.version }}
      # Curated notes, kept in the repository next to the source they describe. The upstream
      # workflow requires this to be a tracked file, so add release-notes/<version>.md before
      # dispatching a release — the run fails closed rather than publishing generated notes.
      release-notes-file: release-notes/${{ inputs.version }}.md
    secrets:
      MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
      MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
      GPG_SECRET_KEY: ${{ secrets.GPG_SECRET_KEY }}
      GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
